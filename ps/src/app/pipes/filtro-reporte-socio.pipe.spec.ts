import { FiltroReporteSocioPipe } from './filtro-reporte-socio.pipe';

describe('FiltroReporteSocioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroReporteSocioPipe();
    expect(pipe).toBeTruthy();
  });
});
