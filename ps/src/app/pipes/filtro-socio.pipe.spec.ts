import { FiltroSocioPipe } from './filtro-socio.pipe';

describe('FiltroSocioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSocioPipe();
    expect(pipe).toBeTruthy();
  });
});
