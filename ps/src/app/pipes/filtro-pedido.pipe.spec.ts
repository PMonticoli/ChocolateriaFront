import { FiltroPedidoPipe } from './filtro-pedido.pipe';

describe('FiltroPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
