import { FiltroReportePromocionPipe } from './filtro-reporte-promocion.pipe';

describe('FiltroReportePromocionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroReportePromocionPipe();
    expect(pipe).toBeTruthy();
  });
});
