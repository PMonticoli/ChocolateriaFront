import { FiltroReportePipe } from './filtro-reporte.pipe';

describe('FiltroReportePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroReportePipe();
    expect(pipe).toBeTruthy();
  });
});
