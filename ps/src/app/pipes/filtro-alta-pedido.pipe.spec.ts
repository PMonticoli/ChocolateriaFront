import { FiltroAltaPedidoPipe } from './filtro-alta-pedido.pipe';

describe('FiltroAltaPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAltaPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
