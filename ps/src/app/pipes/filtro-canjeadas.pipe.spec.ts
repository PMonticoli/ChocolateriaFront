import { FiltroCanjeadasPipe } from './filtro-canjeadas.pipe';

describe('FiltroCanjeadasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCanjeadasPipe();
    expect(pipe).toBeTruthy();
  });
});
