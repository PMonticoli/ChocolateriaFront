import { FiltroReporteCantidadPipe } from './filtro-reporte-cantidad.pipe';

describe('FiltroReporteCantidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroReporteCantidadPipe();
    expect(pipe).toBeTruthy();
  });
});
